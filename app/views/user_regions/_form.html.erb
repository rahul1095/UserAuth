<div class="containner">
<%= form_for(user_region) do |f| %>
  <% if user_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_region.errors.count, "error") %> prohibited this user_region from being saved:</h2>

      <ul>
      <% user_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
     <%= f.label :user %>
    <%= f.select(:user_id, User.all.collect {|u| [u.first_name, u.id]},:prompt => 'Select Category',:class=>"form-control") %>   
</div></br></br>

<div class="field">
     <%= f.label :country %>
    <%= f.select(:region_id, Region.all.collect {|u| [u.region_name, u.id]},:prompt => 'Select Region',:class=>"form-control") %>
    
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>